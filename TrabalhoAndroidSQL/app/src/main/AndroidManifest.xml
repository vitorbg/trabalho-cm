<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="compmovel.trabalhoandroidsql">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddActivity"
            android:label="@string/title_activity_add" />
        <activity
            android:name=".MostraProdutoActivity"
            android:label="@string/title_activity_mostra_produto" />
        <activity
            android:name=".ProdutoActivity"
            android:label="@string/title_activity_produto" />
        <activity
            android:name=".Fornecedores"
            android:label="@string/title_activity_fornecedores"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FornecedoresActivity" />
        <activity android:name=".ServidorActivity"></activity>
    </application>

</manifest>
